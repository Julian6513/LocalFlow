# LocalFlow website

Static landing page for the LocalFlow fork. Serve this directory as a web root if you deploy it. No production domain or downloadable release is claimed here; the main call to action links to the repository build instructions.
